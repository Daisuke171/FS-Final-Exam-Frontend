import LoginCard from "./login-card";

export default function LogIn() {
  return (
    <div className="mt-[calc(75px+2.5rem)] flex items-center justify-center">
      <LoginCard />
    </div>
  );
}
